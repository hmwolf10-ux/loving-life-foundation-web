Small condensed-uppercase pill for program labels, statuses, and category tags.

```jsx
<Badge color="orange" dot>Applications open</Badge>
```

Colors map to the brand palette (`navy`, `orange`, `green`, `sky`) plus `soft` (neutral stone) and `outline` (navy border, transparent fill). Use `dot` for live/active states like "Live now" or "Applications open".
